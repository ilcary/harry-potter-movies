import { DurationMinutesInHoursPipe } from './duration-minutes-in-hours.pipe';

describe('DurationMinutesInHoursPipe', () => {
  it('create an instance', () => {
    const pipe = new DurationMinutesInHoursPipe();
    expect(pipe).toBeTruthy();
  });
});
